Version 1.4

* -r <file> や -R <file> は -r<file> -R<file> と書いても良い。

Version 1.3

* ドキュメントの加筆修正。

* -f オプションが指定されている場合には、ファイル引数が無くてもエラーとしな
  いようにした。

* スタック量を再調整。

* 引数‘-’と‘--’の扱いを変更。

* エラー・メッセージを追加。

* -Rr の reffile を参照時のエラーは -f が指定されていても報告するようにした。

* -Rr の reffile のメディアがプロテクトされているとエラーとなる不具合を修正。

Version 1.2

* 日付が変わる瞬間に実行されたときにも日付を間違えないようにする処理に誤り
  があったのを修正。

* touch.1 の加筆修正。

Version 1.1

* 些細な修正。

Version 1.0

* 作成。

